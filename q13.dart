void main() {
  /*Implement a code that takes in a list of integers and
  returns a new list containing only the unique elements
   from the original list. The order of elements in the 
  new list should be the same as in the original list.
*/
  List<int> num = [1, 2, 3, 4, 2, 3, 1, 5];
  num.removeWhere((num) => num == r.int);
  print(num.append);
}
